#!/bin/sh
#
# SSH Setup
set -e

if [ -z "$1" ];
then
    echo "should specify a script file."
    exit 1
fi

eval $(ssh-agent -s)
echo "$PRIVATE_KEY" | tr -d '\r' | ssh-add - > /dev/null
echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config
sh $1
